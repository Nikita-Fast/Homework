package com.company;

import com.company.algorithms.EquationsSolver;
import com.company.auxiliaries.MyPair;

public class Main {

    public static void main(String[] args) throws InterruptedException {
        /*int LENGTH = 5;
        MyPair[] pairs = new MyPair[LENGTH];
        for (int i = 0; i < LENGTH; i++) {
            pairs[i] = new MyPair(i, i + 1);
        }

        int a = EquationsSolver.solveInParallel(pairs, 8);
        int b = EquationsSolver.solveWithSingleThread(pairs);
        System.out.println(a == b);
        System.out.println(a);*/
    }

}
