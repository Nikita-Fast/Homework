package com.company;import java.awt.geom.Point2D;import static java.lang.Math.cos;import static java.lang.Math.sin;import static java.lang.Math.toRadians;public class Location {    private Point2D point;    private double angle;    public Location() {        point = new MyPoint(0, 0); //angle in degrees        angle = 0; //совпадает с направлением оси Ox    }    public Location(Point2D point, double angle) { //angle in degrees        this.point = point;        this.angle = angle;    }    public void move(double angle, double radius) { //angle in degrees        double x = point.getX() + radius * cos(toRadians(this.angle + angle));        double y = point.getY() + radius * sin(toRadians(this.angle + angle));        point = new MyPoint(x, y);        this.angle += angle;    }    public Location shift(double angle, double radius) {        double x = point.getX() + radius * cos(toRadians(this.angle + angle));        double y = point.getY() + radius * sin(toRadians(this.angle + angle));        return new Location(new MyPoint(x, y), this.angle + angle);    }    public Point2D getPoint() {        return point;    }    public double getAngle() {        return angle;    }    public String toString() {        return point.toString();    }}