package com.company;import static java.lang.Math.cos;import static java.lang.Math.sin;public class MyVector {    private double x;    private double y;    private double angle;    //вектор создаём по углу и длине    public MyVector(double angle, double r) {        this.angle = angle;        final double a = Math.toRadians(angle);        x = r * cos(a);        y = r * sin(a);    }    //вектор создаём по координатам точки и углу    public MyVector(double x, double y, double angle) {        this.x = x;        this.y = y;        this.angle = angle;    }    public MyVector rotate(double angle) { //не меняет значение переменной angle        final double a = Math.toRadians(angle);        double xr = x * cos(a) - y * sin(a);        double yr = x * sin(a) + y * cos(a);        return new MyVector(xr, yr, this.angle);    }    /*    public void setX(double x) {        this.x = x;    }    public void setY(double y) {        this.y = y;    }     */    public double getX() {        return x;    }    public double getY() {        return y;    }    public double getAngle() {        return angle;    }    public String toString() {        return "(" + x + " " + y + ")";    }}